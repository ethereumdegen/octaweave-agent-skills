{
  "name": "octaweave_list_boards",
  "description": "List the workspace's kanban boards with their card counts. Returns {items} of {id, name, slug, description, card_count, updated_at}.",
  "method": "GET",
  "url": "https://octaweave.com/api/v1/w/{workspace}/boards",
  "headers": {
    "Authorization": "Bearer $OCTAWEAVE_API_KEY"
  },
  "parameters": {
    "type": "object",
    "properties": {
      "workspace": {
        "type": "string",
        "description": "Workspace slug or uuid. An API key is pinned to ONE workspace \u2014 call octaweave_whoami and use actor.workspace_id. A wrong workspace is a 403, not a 404."
      }
    },
    "required": [
      "workspace"
    ]
  },
  "body_mapping": "none"
}
