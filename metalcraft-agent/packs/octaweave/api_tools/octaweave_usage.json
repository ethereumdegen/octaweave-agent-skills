{
  "name": "octaweave_usage",
  "description": "Storage used by the workspace against its quota (10 GiB by default). Check this before uploading anything large \u2014 presign refuses once the quota is exceeded.",
  "method": "GET",
  "url": "https://octaweave.com/api/v1/w/{workspace}/usage",
  "headers": {
    "Authorization": "Bearer $OCTAWEAVE_API_KEY"
  },
  "parameters": {
    "type": "object",
    "properties": {
      "workspace": {
        "type": "string",
        "description": "Workspace slug or uuid. An API key is pinned to ONE workspace \u2014 call octaweave_whoami and use actor.workspace_id. A wrong workspace is a 403, not a 404."
      }
    },
    "required": [
      "workspace"
    ]
  },
  "body_mapping": "none"
}
